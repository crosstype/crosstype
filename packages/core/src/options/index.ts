/**
 * Tools to create a option sets - used within language packages, CLI, this package (for base options), etc
 */
export namespace opt {
  export function createCompileOptionSet() {
  }

  export function createParseOptionSet() {
  }
}
